/*using System;

class SumAlgorithm
{
    static void Main()
    {
        int[] scores = { 100, 75, 65, 95, 90, 40, 85, 100 };
        int sum = 0;

        for (int i = 0; i < scores.Length; i++)
        {
            if (scores[i] >= 80)
                sum += scores[i];
        }

        Console.WriteLine($"{scores.Length}명의 점수중 80점 이상점수의 합은: {sum}");
    }
}
*/